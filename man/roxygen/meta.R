
list(
    rd_family_title = list(
        flightData = "Other flight data related topics:",
        danubeDate = "Other Danube data related topics:",
        datasets = "Other datasets:",
        parameterEstimation = "Other parameter estimation methods:",
        structureEstimation = "Other structure estimation methods:",
        exampleGenerations = "Other example generation functions:",
        samplingFunctions = "Other sampling functions:",
        matrixTransformations = "Other parameter matrix transformations:",
        matrixCompletions = "Other matrix completion related topics:",
        getOptions = "Other default parameters:",
        inputChecks = "Other input validation functions:"
    )
)
