# Data definitions

# g is a square, symmetric binary matrix
# interp. an undirected graph (UG)

g <- rbind(c(0, 1, 1, 0),
           c(1, 0, 0, 1),
           c(1, 0, 0, 0),
           c(0, 1, 0, 0))


